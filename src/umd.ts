// entry of the UMD bundle (konva.js): only a default export, so that the
// global is the Konva object itself. index.ts adds the named exports
import { Konva } from './_FullInternals.ts';

export default Konva;
